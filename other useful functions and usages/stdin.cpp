#include <iostream>
#include <string>
#include <typeinfo>
#include <vector>
#include <string.h>
using namespace std;

int main() {

	cout << "=========handle stdin==========" << endl;
	string input;
	string inputNumber;
	getline(cin, input
	int num;
	getline(cin, inputNumber);
	num = stoi(inputNumber);
	return 0;
	
}